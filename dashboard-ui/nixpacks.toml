providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install", "npm install -g serve"]

[phases.build]
cmds = ["npm run build"]
# Frontend rebuild trigger - version 0.1.2

[start]
cmd = "npx serve -s build -p 3000"
